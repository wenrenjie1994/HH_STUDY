一、 命令提示符
1. 进入快捷键：Win+R；
2. 切换盘符：盘符名：；
3. 进入文件夹：cd 文件名；
4. 进入多级文件夹：cd 文件路径；
5. 返回上一级：cd ..；
6. 回根路径：cd \；
7. 文件夹内容显示：dir；
8. 清屏：cls；
9. 退出：exit；
二、 JVM
Java虚拟机，是Java程序的运行环境，实现了Java程序的跨平台特性，每个操作系统有各自专用版本的JVM，相当于针对不同语言的翻译器。
三、 JRE & JDK
1. JRE：Java程序的运行时环境，包含JVM + 核心类库；
2. JDK：Java程序开发工具包，包含JRE + 开发工具；

